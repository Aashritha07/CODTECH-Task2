// Cancel Page Component (React)
import React from 'react';

const Cancel = () => {
    return (
        <div>
            <h1>Payment Canceled</h1>
            <p>Your payment was canceled. You can try again or contact support if you have any issues.</p>
        </div>
    );
};

export default Cancel;
